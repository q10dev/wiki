version: 1.0
runtime: nodejs22

build:
  commands:
    pre-build:
      - corepack enable
    build:
      - yarn install --immutable
      - yarn build

run:
  command: yarn start
  network:
    port: 3000
    env: PORT
